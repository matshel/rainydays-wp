

function errorMessage(message = "Unknown Error") {
    return `<div class="error">${message}</div>`;
}

// function createMessage(type = "sucess", message = "No message") {
//     const html = `<div class="message ${type}">${message}</div>`;
//     return html;
// }